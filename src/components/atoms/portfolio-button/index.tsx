import React from 'react'

const index = ({ text }: { text: string }) => {
    return (
        <div>
            <a href="">
                {text}
            </a>
        </div>
    )
}

export default index